# 🍎 Инструкция по настройке Apple OAuth2

Это подробное руководство по получению всех необходимых ключей и настройке **Sign in with Apple** для вашего приложения.

---

bundleId - com.testmycompany.myapp

Apple_TEAM_ID = 
2UBQYG3N88
APPLE_CLIENT_ID = com.mytestcompany.myapp.service
Key ID
N6WHAVD4ML

## 📋 Что нужно получить

Для работы Apple OAuth2 вам потребуются:

1. **Team ID** - идентификатор вашей команды разработчиков
2. **Client ID** (Services ID) - идентификатор сервиса
3. **Key ID** - идентификатор ключа для Sign in with Apple
4. **Private Key** (.p8 файл) - закрытый ключ для подписи JWT
5. **App ID** - идентификатор приложения (Bundle ID)

---

## 🚀 Шаг 1: Создание App ID

### 1.1. Войдите в Apple Developer Console
- Перейдите на https://developer.apple.com/account
- Войдите с помощью вашего Apple ID (нужна активная подписка Apple Developer)

### 1.2. Создайте App ID
1. Перейдите: **Certificates, Identifiers & Profiles** → **Identifiers**
2. Нажмите кнопку **[+]** (плюс) в правом верхнем углу
3. Выберите **App IDs** и нажмите **Continue**
4. Выберите **App** и нажмите **Continue**
5. Заполните форму:
   - **Description**: `My App` (любое название)
   - **Bundle ID**: `com.yourcompany.yourapp` (уникальный идентификатор)
   - **Explicit**: выберите этот тип
6. В разделе **Capabilities** найдите и включите **Sign in with Apple**
7. Нажмите **Continue** → **Register**

✅ **Сохраните ваш Bundle ID** - он понадобится далее

---

## 🔑 Шаг 2: Создание Services ID (Client ID)

Services ID - это идентификатор для веб-авторизации.

### 2.1. Создайте Services ID
1. Снова перейдите: **Identifiers** → нажмите **[+]**
2. Выберите **Services IDs** и нажмите **Continue**
3. Заполните форму:
   - **Description**: `My App Web Service` (любое название)
   - **Identifier**: `com.yourcompany.yourapp.service` (уникальный идентификатор)
4. Включите чекбокс **Sign in with Apple**
5. Нажмите **Configure** рядом с "Sign in with Apple"

### 2.2. Настройте Redirect URLs
В открывшемся окне:
1. **Primary App ID**: выберите App ID, созданный в Шаге 1
2. **Website URLs**:
   - **Domains and Subdomains**: `localhost` (для разработки)
   - **Return URLs**: `http://localhost:8000/auth/apple/callback`
3. Нажмите **Next** → **Done**
4. Нажмите **Continue** → **Register**

✅ **Сохраните Services ID** - это ваш `APPLE_CLIENT_ID`

**Пример:**
```
APPLE_CLIENT_ID = "com.yourcompany.yourapp.service"
```

---

## 🔐 Шаг 3: Создание Key (Private Key)

Этот ключ используется для подписи JWT токенов.

### 3.1. Создайте новый ключ
1. Перейдите: **Keys** → нажмите **[+]**
2. Заполните форму:
   - **Key Name**: `Sign in with Apple Key` (любое название)
   - Включите чекбокс **Sign in with Apple**
3. Нажмите **Configure** рядом с "Sign in with Apple"
4. Выберите ваш **Primary App ID** (из Шага 1)
5. Нажмите **Save** → **Continue** → **Register**

### 3.2. Скачайте ключ
1. **⚠️ ВАЖНО**: Ключ можно скачать **только один раз**!
2. Нажмите **Download** и сохраните файл `AuthKey_XXXXXXXXXX.p8`
3. Запишите **Key ID** (10-символьный код, например: `AB12CD34EF`)

✅ **Сохраните:**
- Файл `.p8` в безопасном месте
- **Key ID** - это ваш `APPLE_KEY_ID`

**Пример:**
```
APPLE_KEY_ID = "AB12CD34EF"
```

---

## 👥 Шаг 4: Получение Team ID

Team ID - это идентификатор вашей команды разработчиков.

### 4.1. Найдите Team ID
1. В Apple Developer Console перейдите в раздел **Membership**
2. Найдите поле **Team ID** (10-символьный код)
3. Скопируйте его

✅ **Это ваш `APPLE_TEAM_ID`**

**Пример:**
```
APPLE_TEAM_ID = "XY12ZA34BC"
```

---

## 📝 Шаг 5: Заполнение конфигурации

Теперь откройте файл `backend/config.py` и заполните все константы:

```python
# Team ID - скопируйте из Membership
APPLE_TEAM_ID: str = "XY12ZA34BC"  # ← Ваш Team ID

# Client ID - это ваш Services ID
APPLE_CLIENT_ID: str = "com.yourcompany.yourapp.service"  # ← Ваш Services ID

# Key ID - 10-символьный код ключа
APPLE_KEY_ID: str = "AB12CD34EF"  # ← Ваш Key ID

# Private Key - содержимое .p8 файла
APPLE_PRIVATE_KEY: str = """-----BEGIN PRIVATE KEY-----
MIGTAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBHkwdwIBAQQg...
... весь текст из файла AuthKey_XXXXXXXXXX.p8 ...
-----END PRIVATE KEY-----"""  # ← Содержимое вашего .p8 файла

# Redirect URI - должен совпадать с тем, что в Services ID
APPLE_REDIRECT_URI: str = "http://localhost:8000/auth/apple/callback"
```

### 5.1. Как скопировать содержимое .p8 файла

**На Windows:**
```powershell
# Откройте файл в Блокноте
notepad AuthKey_XXXXXXXXXX.p8

# Или выведите в консоль PowerShell
Get-Content AuthKey_XXXXXXXXXX.p8 | Write-Output
```

**На macOS/Linux:**
```bash
# Выведите содержимое в терминал
cat AuthKey_XXXXXXXXXX.p8
```

Скопируйте **весь текст**, включая строки `-----BEGIN PRIVATE KEY-----` и `-----END PRIVATE KEY-----`.

---

## 🚀 Шаг 6: Установка и запуск

### 6.1. Установите зависимости Python
```bash
# Создайте виртуальное окружение (опционально, но рекомендуется)
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Установите зависимости
pip install -r backend/requirements.txt
```

### 6.2. Установите зависимости Node.js
```bash
npm install
```

### 6.3. Запустите приложение

**Вариант 1: Запуск backend и frontend одновременно (рекомендуется)**
```bash
npm run dev:all
```

**Вариант 2: Запуск по отдельности**

Терминал 1 (Backend):
```bash
cd backend
python main.py
# Или: uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Терминал 2 (Frontend):
```bash
npm run dev
```

### 6.4. Откройте браузер
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs (автоматическая документация FastAPI)

---

## ✅ Тестирование конфигурации

Перед тем как пытаться авторизоваться, проверьте что все настроено правильно:

### Тест 1: Проверка конфигурации
```bash
curl http://localhost:8000/auth/test-config
```

Должны увидеть все ваши настройки (без приватного ключа целиком).

### Тест 2: Генерация JWT
```bash
curl http://localhost:8000/auth/test-jwt
```

Если всё настроено правильно, вы получите JWT токен (client_secret).

### Тест 3: Авторизация
1. Откройте http://localhost:3000
2. Нажмите кнопку **Sign in with Apple**
3. Вас перенаправит на страницу Apple
4. Войдите с помощью вашего Apple ID
5. После успешной авторизации вы вернетесь на `/callback` с данными пользователя

---

## 🔒 Безопасность для Production

⚠️ **Важно для production окружения:**

### 1. Используйте HTTPS
Apple требует HTTPS для production. Локально можно использовать HTTP.

### 2. Обновите Redirect URI
В Apple Developer Console добавьте production URL:
- Domains: `yourdomain.com`
- Return URLs: `https://yourdomain.com/auth/apple/callback`

### 3. Переменные окружения
Не храните ключи в коде! Используйте `.env` файл:

```bash
# backend/.env
APPLE_TEAM_ID=XY12ZA34BC
APPLE_CLIENT_ID=com.yourcompany.yourapp.service
APPLE_KEY_ID=AB12CD34EF
APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
APPLE_REDIRECT_URI=https://yourdomain.com/auth/apple/callback
```

### 4. Валидация ID Token
В production обязательно включите полную валидацию токенов с проверкой подписи через Apple Public Keys (см. `utils.py` → `validate_id_token_full`).

### 5. Используйте БД для сессий
Замените in-memory хранилище (`user_sessions`) на Redis или базу данных.

### 6. Обновите CORS
В `main.py` обновите `allow_origins` на ваш production домен.

---

## 🐛 Решение проблем

### Ошибка: "Invalid client"
- Проверьте что `APPLE_CLIENT_ID` совпадает с Services ID
- Убедитесь что Services ID настроен для Sign in with Apple

### Ошибка: "Invalid redirect_uri"
- `APPLE_REDIRECT_URI` в коде должен **точно совпадать** с Return URL в Apple Developer Console
- Проверьте протокол (http vs https)
- Проверьте порт (должен быть указан, если не стандартный)

### Ошибка при генерации JWT (client_secret)
- Проверьте что скопировали **весь** .p8 файл, включая BEGIN/END строки
- Убедитесь что `APPLE_KEY_ID` правильный (10 символов)
- Проверьте что `APPLE_TEAM_ID` правильный (10 символов)

### Email не приходит при повторных входах
- Apple передает email и имя **только при первой авторизации**
- При повторных входах вы получите только `sub` (user ID)
- Для тестирования: удалите приложение из списка авторизованных в настройках Apple ID

### Как сбросить авторизацию для тестирования
1. iPhone/iPad: Настройки → Apple ID → Пароль и безопасность → Приложения, использующие Apple ID
2. Mac: Системные настройки → Apple ID → Пароль и безопасность
3. Найдите ваше приложение и удалите доступ
4. Теперь при новой авторизации снова запросится email и имя

---

## 📚 Дополнительные ресурсы

- [Официальная документация Apple](https://developer.apple.com/documentation/sign_in_with_apple)
- [REST API документация](https://developer.apple.com/documentation/sign_in_with_apple/sign_in_with_apple_rest_api)
- [Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/sign-in-with-apple)

---

## 📧 Структура проекта

```
apple-id/
├── app/                        # Next.js frontend
│   ├── page.tsx               # Главная страница с кнопкой авторизации
│   ├── callback/
│   │   └── page.tsx          # Обработка callback после авторизации
│   └── ...
├── backend/                   # Python FastAPI backend
│   ├── main.py               # Основной сервер с endpoints
│   ├── config.py             # Конфигурация (заполните ключи здесь!)
│   ├── utils.py              # Утилиты (генерация JWT, валидация)
│   └── requirements.txt      # Python зависимости
├── package.json              # Node.js зависимости и скрипты
└── APPLE_SETUP.md           # Эта инструкция
```

---

## ✅ Чеклист перед запуском

- [ ] Создан App ID в Apple Developer Console
- [ ] Создан Services ID с настроенными Redirect URLs
- [ ] Создан и скачан Private Key (.p8 файл)
- [ ] Получен Team ID
- [ ] Заполнены все константы в `backend/config.py`
- [ ] Установлены Python зависимости (`pip install -r backend/requirements.txt`)
- [ ] Установлены Node.js зависимости (`npm install`)
- [ ] Backend успешно запускается (`python backend/main.py`)
- [ ] Frontend успешно запускается (`npm run dev`)
- [ ] Тесты `/auth/test-config` и `/auth/test-jwt` проходят успешно

---

## 🎉 Готово!

Теперь у вас есть полностью рабочая референсная реализация **Sign in with Apple** с Python backend и Next.js frontend!

Этот код можно адаптировать под любой другой backend (Django, Flask, FastAPI) и любой frontend (React, Vue, Angular).

**Удачи в разработке!** 🚀


