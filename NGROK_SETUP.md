# 🚇 Запуск проекта через ngrok

Подробная инструкция по запуску Apple OAuth2 проекта с использованием ngrok для обхода ограничений localhost в Apple Developer Console.

---

## ✅ Что уже сделано

- ✅ Установлены Node.js зависимости
- ✅ Создано виртуальное окружение Python (venv)
- ✅ Установлены все Python зависимости
- ✅ Установлен ngrok

---

## 🚀 Пошаговая инструкция по запуску

### Шаг 1: Зарегистрируйтесь на ngrok (опционально, но рекомендуется)

1. Перейдите на https://dashboard.ngrok.com/signup
2. Зарегистрируйтесь (бесплатно)
3. Получите authtoken в https://dashboard.ngrok.com/get-started/your-authtoken
4. Настройте authtoken (выполните один раз):

```bash
ngrok config add-authtoken YOUR_AUTHTOKEN_HERE
```

**Зачем нужна регистрация?**
- Без регистрации: ограничения на количество подключений, короткие сессии
- С регистрацией: стабильные туннели, больше лимитов, бесплатно

---

### Шаг 2: Заполните константы в backend/config.py

Откройте `backend/config.py` и **временно** заполните базовые значения:

```python
# Team ID - из Apple Developer Console > Membership
APPLE_TEAM_ID: str = "YOUR_TEAM_ID"

# Client ID - из Services ID (будет создан в Шаге 4)
APPLE_CLIENT_ID: str = "com.testmycompany.myapp.service"

# Key ID - из Apple Developer Console > Keys
APPLE_KEY_ID: str = "YOUR_KEY_ID"

# Private Key - содержимое .p8 файла
APPLE_PRIVATE_KEY: str = """-----BEGIN PRIVATE KEY-----
YOUR_PRIVATE_KEY_CONTENT
-----END PRIVATE KEY-----"""

# Redirect URI - ПОКА оставьте так, обновим после получения ngrok URL
APPLE_REDIRECT_URI: str = "http://localhost:8000/auth/apple/callback"
```

**Важно:** Team ID, Key ID и Private Key возьмите из Apple Developer Console по инструкции в `APPLE_SETUP.md` (Шаги 1, 3, 4).

---

### Шаг 3: Запустите backend локально

**Терминал 1 - Backend:**

```bash
# Активируйте venv
venv\Scripts\activate

# Перейдите в папку backend
cd backend

# Запустите сервер
python main.py
```

Вы должны увидеть:
```
🚀 Запуск Apple OAuth2 Backend...
📍 Backend URL: http://localhost:8000
...
INFO:     Uvicorn running on http://0.0.0.0:8000
```

**Оставьте этот терминал открытым!**

---

### Шаг 4: Запустите ngrok туннель

**Терминал 2 - ngrok:**

```bash
ngrok http 8000
```

Вы увидите что-то вроде:

```
Session Status                online
Account                       your@email.com (Plan: Free)
Version                       3.3.0
Region                        United States (us)
Latency                       -
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc123def456.ngrok-free.app -> http://localhost:8000

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

**📝 Важно:** Скопируйте URL вида `https://abc123def456.ngrok-free.app` - это ваш публичный URL!

**Оставьте этот терминал открытым!**

---

### Шаг 5: Настройте Apple Developer Console

Теперь, когда у вас есть ngrok URL, настройте Apple:

1. Перейдите в https://developer.apple.com/account
2. **Certificates, Identifiers & Profiles** → **Identifiers** → ваш **Services ID**
3. Нажмите **Configure** рядом с "Sign in with Apple"
4. Заполните:

```
Domains and Subdomains: abc123def456.ngrok-free.app
(без https://, только домен)

Return URLs: https://abc123def456.ngrok-free.app/auth/apple/callback
(полный URL с https://)
```

5. Нажмите **Next** → **Done** → **Save**

**⚠️ Важно:** Используйте HTTPS (с буквой S)! ngrok автоматически дает HTTPS.

---

### Шаг 6: Обновите backend/config.py с ngrok URL

Откройте `backend/config.py` и обновите:

```python
# Redirect URI - замените на ваш ngrok URL
APPLE_REDIRECT_URI: str = "https://abc123def456.ngrok-free.app/auth/apple/callback"

# Обновите URLs
BACKEND_URL: str = "https://abc123def456.ngrok-free.app"
FRONTEND_URL: str = "http://localhost:3000"
```

**Перезапустите backend** (Ctrl+C в Терминале 1, затем снова `python main.py`)

---

### Шаг 7: Обновите frontend с ngrok URL

Откройте `app/page.tsx` и измените константу `BACKEND_URL`:

```typescript
const BACKEND_URL = 'https://abc123def456.ngrok-free.app';
```

---

### Шаг 8: Запустите frontend

**Терминал 3 - Frontend:**

```bash
npm run dev
```

Откройте браузер: http://localhost:3000

---

### Шаг 9: Тестирование

1. Откройте http://localhost:3000
2. Нажмите **"Sign in with Apple"**
3. Вас перенаправит на Apple (через ngrok)
4. Войдите с Apple ID
5. После успешной авторизации вы вернетесь на страницу callback с данными пользователя

**🎉 Если все работает - поздравляю!**

---

## 🔍 Проверка конфигурации

Перед тестом авторизации проверьте настройки:

```bash
# Тест 1: Проверка конфигурации (используйте ngrok URL)
curl https://abc123def456.ngrok-free.app/auth/test-config

# Тест 2: Генерация JWT
curl https://abc123def456.ngrok-free.app/auth/test-jwt
```

Если оба теста прошли успешно - можно авторизоваться!

---

## 📊 Мониторинг запросов

ngrok предоставляет Web UI для мониторинга всех запросов:

Откройте: http://127.0.0.1:4040

Здесь вы увидите все HTTP запросы, которые проходят через туннель. Очень полезно для отладки!

---

## 🔄 Что делать при перезапуске ngrok?

**Проблема:** Каждый раз при перезапуске ngrok генерирует **новый URL** (в бесплатной версии).

**Решение:**

1. Скопируйте новый ngrok URL
2. Обновите в **Apple Developer Console** (Return URLs)
3. Обновите в `backend/config.py` (`APPLE_REDIRECT_URI`, `BACKEND_URL`)
4. Обновите в `app/page.tsx` (`BACKEND_URL`)
5. Перезапустите backend
6. Обновите страницу в браузере (frontend подхватит изменения автоматически)

**Чтобы избежать изменений URL:**
- Купите ngrok Pro ($8/месяц) - даёт фиксированный домен
- Или используйте бесплатные альтернативы с фиксированным доменом: localtunnel, serveo

---

## 🐛 Решение проблем

### Ошибка: "ERR_NGROK_3200" или "Visit Site"

При первом открытии ngrok URL браузер может показать предупреждение.
- Нажмите **"Visit Site"**
- Это нормально для бесплатного ngrok

### Backend не доступен через ngrok

Проверьте:
```bash
# Локально работает?
curl http://localhost:8000/

# Через ngrok работает?
curl https://abc123def456.ngrok-free.app/
```

### Ошибка "Invalid redirect_uri" от Apple

- Проверьте что URL в `config.py` **точно совпадает** с Return URL в Apple Console
- Должен быть HTTPS (с буквой S)
- Не забудьте `/auth/apple/callback` в конце

### ngrok команда не найдена

```bash
# Проверьте установку
npm list -g ngrok

# Переустановите
npm install -g ngrok

# Или скачайте с сайта
# https://ngrok.com/download
```

---

## 💡 Альтернативы ngrok

Если ngrok не подходит:

### 1. localtunnel (бесплатный, без регистрации)
```bash
npm install -g localtunnel
lt --port 8000
```

### 2. serveo (бесплатный, через SSH)
```bash
ssh -R 80:localhost:8000 serveo.net
```

### 3. Cloudflare Tunnel (бесплатный)
```bash
cloudflared tunnel --url http://localhost:8000
```

---

## 📝 Краткая шпаргалка по командам

```bash
# Терминал 1 - Backend
venv\Scripts\activate
cd backend
python main.py

# Терминал 2 - ngrok
ngrok http 8000

# Терминал 3 - Frontend
npm run dev
```

**Порядок запуска:**
1. Backend
2. ngrok
3. Frontend

---

## ✅ Чеклист перед тестом

- [ ] Backend запущен и работает на http://localhost:8000
- [ ] ngrok запущен и показывает Forwarding URL
- [ ] Скопирован ngrok URL (https://...)
- [ ] Apple Developer Console обновлён с ngrok URL
- [ ] `backend/config.py` обновлён с ngrok URL
- [ ] `app/page.tsx` обновлён с ngrok URL
- [ ] Backend перезапущен после изменения config.py
- [ ] Frontend запущен на http://localhost:3000
- [ ] Тесты `/auth/test-config` и `/auth/test-jwt` проходят

---

## 🎓 Что дальше?

После того как авторизация заработает через ngrok:

1. **Для продакшена:** замените ngrok на реальный домен с HTTPS
2. **Для долгой разработки:** купите ngrok Pro для фиксированного домена
3. **Для команды:** поделитесь ngrok URL с коллегами для тестирования

---

**Готово! Теперь у вас всё настроено для работы с Apple OAuth2 через ngrok.** 🚀

Если возникнут вопросы - смотрите секцию "Решение проблем" выше.

